# ScholarFlow 2.5.1 — Feature Overview / Danh sách tính năng

> Hỗ trợ trình duyệt: Firefox & Chromium (Manifest V3) · 5 ngôn ngữ: vi / en / zh / ru / ja
> Browser support: Firefox & Chromium (Manifest V3) · 5 languages: vi / en / zh / ru / ja
> Toàn bộ xử lý diễn ra cục bộ trên máy; không máy chủ, không theo dõi. / All processing is local; no server, no tracking.

| Nhóm / Group | English | Tiếng Việt |
|---|---|---|
| **Citations — Trích Dẫn** | 10 citation styles: IEEE, APA 7th, Harvard, MLA 9th, Vancouver, Chicago 17, ACS, AMA, BibTeX and in-text, each following documented formatting rules for every document type. | 10 chuẩn trích dẫn: IEEE, APA 7th, Harvard, MLA 9th, Vancouver, Chicago 17, ACS, AMA, BibTeX và trích dẫn trong bài, mỗi chuẩn tuân theo quy tắc định dạng cho từng loại tài liệu. |
| | Document types: journal article, conference paper, book, book chapter, thesis/dissertation, technical report, software, video and web page. | Loại tài liệu: bài báo, hội thảo, sách, chương sách, luận văn, báo cáo kỹ thuật, phần mềm, video và trang web. |
| | Export: BibTeX (.bib), RIS (.ris), plain text, EndNote XML (.xml) and CSL-JSON (.json) for the whole library. | Xuất tài liệu: BibTeX (.bib), RIS (.ris), văn bản thường, EndNote XML (.xml) và CSL-JSON (.json) cho toàn danh mục. |
| | Source detection: DOI and meta-tag extraction, plus domain-based type detection (arxiv, pubmed, springer, openreview, github and others). | Nhận diện nguồn: trích xuất DOI và thẻ meta, kèm nhận diện loại nguồn theo tên miền (arxiv, pubmed, springer, openreview, github...). |
| | Autofill metadata from Crossref / OpenAlex with live suggestions. | Tự động tìm và điền metadata từ Crossref / OpenAlex với gợi ý trực tiếp. |
| | Bibliography library with search and sorting (newest, year, name, tag). | Thư viện tài liệu có tìm kiếm và sắp xếp (mới lưu, năm, tên, tag). |
| | YouTube citation data (channel, publish date, duration) merged from three sources. | Dữ liệu trích dẫn YouTube (kênh, ngày đăng, thời lượng) hợp nhất từ ba nguồn. |
| **AI Assistant — AI Trợ lý** | Providers: Gemini, ChatGPT, Claude, custom URL, and local servers (Ollama, LM Studio, vLLM, OpenRouter) with a unified model selector. | Nhà cung cấp: Gemini, ChatGPT, Claude, URL tùy chỉnh và máy chủ cục bộ (Ollama, LM Studio, vLLM, OpenRouter) với ô chọn model thống nhất. |
| | 13+ slash-command skills (/code, /table, /quiz, /critique, /mindmap, /math...) with natural-intent detection and 5 orchestration pipelines. | 13+ kỹ năng lệnh gạch chéo (/code, /table, /quiz, /critique, /mindmap, /math...) với nhận diện ý định tự nhiên và 5 quy trình điều phối. |
| | 3-tier page extraction (Shadow DOM / iframe, isolated scripting, background fetch) with Readability and JSON-LD articleBody scoring. | Trích xuất trang 3 tầng (Shadow DOM / iframe, scripting cách ly, fetch nền) với chấm điểm Readability và JSON-LD articleBody. |
| | Gemini native Google Search grounding with a strict fact rule and source URLs; multi-source consensus search with follow-up suggestions. | Grounding Google Search gốc của Gemini với quy tắc kiểm chứng thông tin và URL nguồn; tìm kiếm đối chiếu đa nguồn kèm gợi ý hỏi tiếp. |
| | Multi-window RAG and pinned pages that join every prompt. | RAG đa cửa sổ và ghim trang web tham gia vào mọi câu hỏi. |
| | Streaming responses (SSE) with Stop and Regenerate; chat history retention and copy/edit. | Trả lời dạng dòng thời gian thực (SSE) kèm nút Dừng và Tạo lại; lưu lịch sử hội thoại và sao chép/chỉnh sửa. |
| | Image attachment up to 4 MB, including Ctrl+V paste. | Đính kèm ảnh tới 4 MB, gồm dán ảnh bằng Ctrl+V. |
| | YouTube: server-side watch-URL transcript, clickable timestamps, resume points, and timeline summaries for long videos. | YouTube: transcript qua watch-URL từ máy chủ, timestamp bấm được, điểm tiếp tục và tóm tắt dòng thời gian cho video dài. |
| | Pipeline status renderer: typing label, step counter and progress bar; single-row auto-growing input. | Bộ hiển thị trạng thái quy trình: nhãn đang nhập, bộ đếm bước và thanh tiến trình; ô nhập một dòng tự giãn. |
| | Markdown rendering (tables, headings, lists, blockquotes, highlight, links) in answers. | Hiển thị markdown trong câu trả lời (bảng, tiêu đề, danh sách, trích dẫn, highlight, liên kết). |
| **Flow (P2P)** | Encrypted WebRTC peer-to-peer file transfer without an intermediate server. | Truyền file P2P bằng WebRTC có mã hóa, không qua máy chủ trung gian. |
| | Data chunking (64 KB) with a real-time progress bar; room code and QR pairing; real-time audio/video calls. | Chia dữ liệu thành khối 64 KB kèm thanh tiến trình; tạo phòng bằng mã và QR; gọi âm thanh/video trực tiếp. |
| | Pop-out window keeps transfers and calls running when the sidebar is closed. | Cửa sổ nổi duy trì truyền file và cuộc gọi khi đóng sidebar. |
| **QR Code — Mã QR** | Art QR with Apple Squircle finder eyes; tree-ring concentric style; standard ISO QR. | QR nghệ thuật với mắt định vị Apple Squircle; kiểu vòng cây đồng tâm; QR chuẩn ISO. |
| | Photo QR using an uploaded image as background; center logo, emoji or initials; 3 color themes; 3D tilt card effect. | QR ảnh dùng ảnh tải lên làm nền; biểu tượng trung tâm là logo, emoji hoặc chữ viết tắt; 3 chủ đề màu; hiệu ứng thẻ 3D. |
| | Export PNG, copy to clipboard, copy link, and open in a fullscreen tab. | Xuất PNG, sao chép ảnh, sao chép link và mở tab toàn màn hình. |
| **Redaction — Che Mờ** | Mask modes: blur, black box, pixelate and hide; masks persist per site and are re-applied after reload. | Các chế độ che: làm mờ, hộp đen, điểm ảnh và ẩn; mask lưu theo trang và tự tái áp sau khi tải lại. |
| | Drag-to-draw precise region masks; auto-scan of sensitive data (email, phone, national ID, card numbers, IP); keyword-based masking. | Kéo vẽ vùng che chính xác; tự quét và che dữ liệu nhạy cảm (email, số điện thoại, CCCD, số thẻ, IP); che theo từ khóa. |
| | Masks are applied to exported screenshots with canvas pixelation; PIN lock for "Show original" (SHA-256 salted hash). | Mask được áp lên ảnh chụp xuất ra với kỹ thuật điểm ảnh bằng canvas; khóa PIN cho nút "Xem bản gốc" (băm SHA-256 có salt). |
| **Capture & Record — Chụp & Quay** | Screenshots: visible area, full page, selection. | Chụp ảnh: vùng nhìn thấy, toàn trang, vùng chọn. |
| | High-quality screen recording and JPEG export; stall guard for iframes and nested scrolling. | Quay màn hình chất lượng cao và xuất JPEG sắc nét; cơ chế chống treo khi chụp trong iframe và cuộn lồng. |
| **Cookie Manager** | Import/export cookies as .json and cookie string; Netscape cookies.txt export for curl/wget. | Xuất/nhập cookie dạng .json và chuỗi; xuất Netscape cookies.txt dùng cho curl/wget. |
| | Detailed list with view/edit/delete/copy per cookie; per-domain cookie profiles. | Danh sách chi tiết xem/sửa/xóa/sao chép từng cookie; hồ sơ cookie theo từng tên miền. |
| **Autofill — Điền Data** | Search and auto-fill source metadata from Crossref / OpenAlex. | Tìm và tự điền metadata nguồn từ Crossref / OpenAlex. |
| **Todo List** | Task list linked to the study flow: goal → source → note → to-do. | Danh sách việc cần làm liên kết chu trình học: mục đích → nguồn → ghi chú → việc cần làm. |
| **Pomodoro** | Focus timer 25/50/5 with session goals; break planner with four one-tap presets and an expected end time. | Timer tập trung 25/50/5 với mục tiêu phiên; bộ lập kế hoạch nghỉ với 4 chế độ một chạm và giờ kết thúc dự kiến. |
| | Break music in multiple styles; floating window with a live progress ring and status. | Nhạc nghỉ đa phong cách; cửa sổ nổi có vòng tiến trình và trạng thái thời gian thực. |
| **Study Calendar & Notes — Lịch & Ghi chú** | Calendar with manual events (color, repeat) and ICS subscription. | Lịch với sự kiện tạo thủ công (màu sắc, lặp lại) và đăng ký ICS. |
| | Research notes in markdown with formula insertion (MathML / LaTeX). | Ghi chú nghiên cứu dạng markdown với chèn công thức (MathML / LaTeX). |
| **Tab Manager** | List, search, sort, group and pin tabs; close duplicates; bookmark all; discard tabs. | Liệt kê, tìm kiếm, sắp xếp, gom nhóm và ghim tab; đóng tab trùng; bookmark tất cả; hủy tải tab. |
| | Save/restore tab sessions; export MD/TXT; auto-refresh when tabs close in the browser. | Lưu/phục hồi phiên tab; xuất MD/TXT; tự cập nhật khi tab đóng từ trình duyệt. |
| **Test Helper** | Record/replay actions, link crawl and check, form fuzzing, accessibility and performance checks; bug reports with a zip of screenshots. | Ghi và phát lại thao tác, quét và kiểm tra link, kiểm thử form, kiểm tra khả năng truy cập và hiệu năng; báo cáo lỗi kèm zip ảnh. |
| **Security** | Phishing and typo-squat detection, clickjacking blocking, site-based right-click options. | Phát hiện phishing và giả mạo tên miền, chặn clickjacking, mở chuột phải theo từng trang. |
| | Trust report: 12-layer heuristic scoring on a 0–10 risk scale, including a brand list of Vietnamese banks and e-wallets. | Báo cáo uy tín: chấm điểm heuristic 12 lớp theo thang rủi ro 0–10, gồm danh sách ngân hàng và ví điện tử Việt Nam. |
| | Auto-decline cookie banners on 16+ consent platforms (one-way action: never clicks Accept). | Tự từ chối banner cookie trên 16+ nền tảng đồng ý cookie (một chiều: không bao giờ bấm Accept). |
| | Cookie audit by domain with tracker/analytics/functional classification and Secure / HttpOnly / SameSite flags. | Kiểm tra cookie theo tên miền, phân loại theo dõi/phân tích/chức năng và cờ Secure / HttpOnly / SameSite. |
| | Password generator, warnings when pasting sensitive numbers, quick links to HaveIBeenPwned / WebRTC / DNS-leak checks. | Trình tạo mật khẩu, cảnh báo khi dán số nhạy cảm, phím tắt kiểm tra HaveIBeenPwned / WebRTC / rò rỉ DNS. |
| **Social Protection — Bảo vệ mạng xã hội** | Unified anti-injection shield: foreign extension scripts, data:/blob: obfuscated scripts, unknown iframes and javascript: links; inline self-XSS theft detection; remove vs record-only modes. | Khiên chống chèn tập nhất: script lạ từ extension khác, script dạng data:/blob: che giấu, iframe lạ và link javascript:; phát hiện mã self-XSS; hai chế độ gỡ ngay hoặc chỉ ghi nhận. |
| | Click-time link cleaner for tracking parameters (utm_*, fbclid, gclid, igshid...) including nested l.php wrappers. | Làm sạch tham số theo dõi khi bấm link (utm_*, fbclid, gclid, igshid...) kể cả wrapper l.php lồng nhau. |
| | Shop / affiliate link remover in comments (Shopee, Lazada, Tiki, Sendo, TikTok Shop, Temu, AliExpress) with preview-card removal; safe social/news links are excluded. | Gỡ link quảng cáo / shop trong bình luận (Shopee, Lazada, Tiki, Sendo, TikTok Shop, Temu, AliExpress) kèm xóa thẻ preview; link mạng xã hội/báo chí được loại trừ. |
| | In-tab statistics and reset for injections removed, links cleaned and shop links removed. | Dòng thống kê và nút reset ngay trong tab: số lần chặn chèn, số link dọn và số link shop gỡ. |
| | Recovery wizard: 8 breach scenarios × 9 platforms with official links and scam warnings; export instructions as .txt. | Wizard khôi phục tài khoản: 8 kịch bản bị chiếm × 9 nền tảng kèm link chính thức và cảnh báo lừa đảo; xuất hướng dẫn .txt. |
| | Encrypted recovery vault: AES-256-GCM with PBKDF2 150k iterations for backup email/phone, trusted contacts and 2FA codes; auto-lock on hide. | Két sắt khôi phục mã hóa AES-256-GCM, PBKDF2 150k vòng cho email/SĐT dự phòng, trusted contacts và mã 2FA; tự khóa khi ẩn. |
| | 10-item defense checklist and a 0–100 defense score. | Checklist phòng thủ 10 mục và điểm phòng thủ 0–100. |
| | Quick PIN lock for the vault and security settings (SHA-256 salted, no plaintext storage). | Khóa nhanh bằng PIN cho két sắt và cài đặt bảo mật (SHA-256 có salt, không lưu bản rõ). |
| | Creator workspace (KOL/KOC): post schedule by platform, views/likes/shares/comments metrics, KPI CSV export, group/channel management. | Không gian sáng tạo (KOL/KOC): lên lịch bài đăng theo nền tảng, ghi chỉ số views/likes/shares/comments, xuất CSV KPI, quản lý Group/Kênh. |
| | Gambling shield: network-layer blocking for roughly 65 Vietnamese betting/casino domains plus any domain containing "casino", including embedded gambling iframes. | Lá chắn cờ bạc: chặn ở tầng mạng khoảng 65 tên miền cá độ/casino hướng Việt Nam và mọi tên miền chứa "casino", kể cả iframe quảng cáo cờ bạc nhúng trong trang khác. |
| | Content fingerprint with a self-learning local domain store: detected pages are blocked on site, then the domain is added to network rules so the same bookmaker is blocked from the start next time (local list, capped). | Quét vân tay nội dung với kho tên miền tự học cục bộ: trang bị nhận diện bị chặn tại chỗ, sau đó tên miền được thêm vào quy tắc mạng để lần sau chặn ngay từ đầu (danh sách cục bộ, giới hạn số lượng). |
| | Localized 5-language block page with the blocked domain, remediation links and a per-domain unblock option. | Trang chặn bản địa hóa 5 ngôn ngữ hiện tên miền bị chặn, đường dẫn xử lý và nút bỏ chặn từng tên miền. |
| **Lingua Lab** | Language learning built on SLA foundations: i+1 comprehensible input, SM-2 spaced repetition, output practice, targeted corrective feedback and the lexical approach. | Học ngoại ngữ dựa trên nền tảng SLA: đầu vào dễ hiểu i+1, lặp lại cách quãng SM-2, luyện viết, phản hồi lỗi có phân loại và học theo cụm từ. |
| | Sentence mining on any page: select text → AI builds L2→L2 cards (explanations in the target language, no native-language translation) with examples, cloze and collocations. | Sentence mining mọi trang: bôi đen → AI tạo thẻ chuẩn L2→L2 (giải thích bằng chính ngôn ngữ đang học, không dịch về tiếng mẹ) kèm ví dụ, cloze và collocations. |
| | Grammar coach with a categorized error ledger (linking words, articles, prepositions, tenses, agreement, collocation, spelling); one button generates drill cards from your own errors. | Grammar coach với sổ lỗi phân loại (liên từ, mạo từ, giới từ, thì, hòa hợp, collocation, chính tả); một nút sinh thẻ luyện từ chính lỗi của bạn. |
| | Bank of 30 academic linking words (however, therefore, moreover, although...) grouped by function with offline gap-fill drills. | Ngân hàng 30 liên từ học thuật (however, therefore, moreover, although...) phân nhóm theo chức năng với bài điền chỗ trống offline. |
| | Listening with the browser's local TTS; streak and daily goals; separate decks per target language (en, es, fr, de, ja, zh, ru). | Nghe bằng TTS cục bộ của trình duyệt; streak và mục tiêu ôn mỗi ngày; bộ thẻ riêng theo từng ngôn ngữ đích (en, es, fr, de, ja, zh, ru). |
| | Write-check pill on any page: user-triggered check of English input of 40+ characters in comments/textarea/contenteditable, with the result returned in a bubble at the cursor. | Chíp kiểm tra viết trên mọi trang: bấm để chấm đoạn tiếng Anh từ 40 ký tự trong comment/textarea/contenteditable, kết quả hiện trong bong bóng tại chỗ. |
| | Offline dictation: local TTS reads a sentence, you type it back; per-word diff with a percentage score and misspelled words recorded in the spelling ledger. | Chép chính tả offline: TTS cục bộ đọc câu, bạn gõ lại; đối chiếu từng từ tô màu kèm điểm phần trăm, từ sai tự ghi vào sổ lỗi chính tả. |
| | AI linker rewriter: rewrites choppy sentences into academic phrasing with however/therefore/although... plus a "Use this version" button. | AI nối câu: biến vài câu cụt thành văn học thuật với however/therefore/although... kèm nút "Dùng bản này". |
| **Dark Mode Studio — Tab Dark** | Engine starts at page load (document_start) so white flashes do not appear; dark-native pages (GitHub, YouTube...) are not inverted twice. | Engine chạy từ lúc trang tải (document_start) để không xuất hiện chớp trắng; trang vốn tối (GitHub, YouTube...) không bị invert hai lần. |
| | Night Reader: safe reading tones layered on top of invert without changing layout — text warmth (0–100%), extra black depth (0–60%) with image compensation, optional link color. | Night Reader: tông màu đọc an toàn xếp lên engine invert, không đụng bố cục — ấm chữ (0–100%), nền tối thêm (0–60%) tự bù ảnh, màu liên kết tùy chọn. |
| | Three presets: News Sepia, AMOLED, Midnight Ocean; the legacy flat recolor is kept as an opt-in switch with a warning. | Ba bộ màu: Sepia tin tức, AMOLED, Midnight ocean; chế độ tô màu phẳng cũ giữ lại như công tắc chọn thêm kèm cảnh báo. |
| | Typography: 10 fonts, reading column width and justified text. | Typography: 10 font, chiều rộng cột đọc và căn lề hai bên. |
| | Per-site tuning: brightness, contrast, color saturation, forced dark, page text size/color, and an exclusion list. | Chỉnh theo trang: độ sáng, tương phản, độ màu, ép tối, cỡ và màu chữ trang, danh sách loại trừ. |
| **Interface — Giao diện** | Header layout customization: position (top or bottom), per-item show/hide and side/order for brand, language, trust and badge. | Tùy chỉnh bố cục thanh đầu: vị trí trên/dưới, hiện/ẩn từng mục và vị trí, thứ tự cho brand, ngôn ngữ, uy tín và badge. |
| | Tab navigation reorder with reset; the chosen order and the last active tab are remembered. | Sắp xếp lại thanh điều hướng với nút khôi phục; thứ tự và tab đang mở được ghi nhớ. |
| **Privacy — Quyền riêng tư** | Local processing: no server, no analytics, no tracking; AI calls, network blocks and content checks run only when you take action. | Xử lý cục bộ: không máy chủ, không phân tích, không theo dõi; gọi AI, chặn mạng và kiểm tra nội dung chỉ chạy khi bạn thao tác. |
| | Data stays in browser storage; optional host permissions are requested only when a feature is used. | Dữ liệu nằm trong bộ nhớ trình duyệt; quyền truy cập trang tùy chọn chỉ được yêu cầu khi dùng tính năng. |